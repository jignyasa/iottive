<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Text.14sp.colorText" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textSize">@dimen/text14sp</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Text.16sp.colorText" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">@dimen/text16sp</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Text.14sp.White" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">@dimen/text14sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="Text.20sp.White" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textSize">@dimen/text20sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>